{
  "name": "mailbento-mcp",
  "version": "0.1.0",
  "description": "MailBento 메일함·메일·보관함을 읽고 표식을 다는 MCP 서버 (stdio)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "mailbento-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
