-- 수집기가 새 메일의 본문을 미리 받아 담기 시작한다. 사람이 열어서 담은 것과
-- 한 표에 섞이는데, 순수한 LRU 로 두면 미리 받은 것이 사람이 열어 본 것을
-- 밀어낸다. 갈라 세려면 표식이 있어야 한다.
--
-- 이미 담겨 있는 행은 전부 사람이 열어서 담긴 것이다 → 기본값 0 이 곧 참이다.
-- (0013 과 달리 표를 비우지 않는다. 지금 담긴 본문은 그대로 쓸 수 있다.)
ALTER TABLE `message_body_cache` ADD `prefetched` integer DEFAULT 0 NOT NULL;
