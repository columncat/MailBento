# MailBento — 인수인계

코드를 처음 여는 사람이 **왜 이렇게 되어 있는지**를 먼저 알 수 있게 쓴 문서다.
무엇을 하는지는 코드가 말해 준다. 여기에는 코드만 봐서는 알 수 없는 결정과 함정을 적는다.

---

## 1. 한 줄 요약

여러 IMAP 메일함을 한 화면에 늘어놓고, 옆에 시계·검색·번역·폴더·메모·코크보드
위젯을 붙인 개인 대시보드. Synology NAS 의 Docker 로 돌고, 자매 앱 **MemoBento** 와
일부 데이터를 공유한다.

---

## 2. 지형도

```
src/
  app/
    api/
      mail/                 메일 목록·단일 메일 (캐시 + 표식 병합)
      archive/              메일 보관함 (사본)
      accounts/ config/ widget/ export/ import/
    settings/               계정·위젯·백업
  components/
    dashboard.tsx           레이아웃의 중심. 위젯 ON/OFF, 넓은 화면 2×2 그리드
    inbox-card.tsx          메일함 카드 (머리말 = 순서 손잡이)
    message-modal.tsx       메일 열람 (라이브 / 보관본 양쪽)
    widget-side-wing.tsx    보관함(왼쪽 열 전체) · Memo · Corkboard 배치
    widget-archive.tsx      메일 보관함 위젯
    widget-*.tsx            시계 / 검색 / 번역 / 폴더
  lib/
    providers/imap.ts       IMAP 구현 (imapflow + mailparser)
    imap-client.ts          IMAP 연결을 여는 일만 (풀이 순환 없이 쓰라고 분리)
    imap-pool.ts            계정+폴더당 최대 3 연결. 본문·첨부·인라인이 함께 쓴다
    mail-part.ts            첨부·인라인 조각 하나 꺼내기 (스트림)
    mail-cache.ts           stale-while-revalidate 캐시
    message-flags.ts        앱 내부 읽음/표식
    archive-server.ts       보관 사본 저장·조회·순서
    message-detail-cache.ts 본문 캐시 (디스크 100통. 사람 몫 70 / 미리 받기 30)
    mail-poller.ts          새 메일 수집 → 에이전트 넘기기 → 본문 미리 받기
    widget-server.ts        widget_state JSON
    crypto.ts               계정 비밀번호 암복호화
```

---

## 3. 반드시 알고 있어야 하는 것

### 3.1 `accounts` 는 "계정"이 아니라 "뷰"다

같은 자격증명으로 행을 복제해 `query` 만 다르게 주면 같은 메일함의 다른 뷰가 된다
(`/api/accounts/[id]/duplicate`). 그래서 계정 행이 늘어나는 것은 정상이고,
계정 삭제가 곧 "메일함 삭제"를 뜻하지 않는다.

### 3.2 보관함은 **참조가 아니라 사본**이다

IMAP UID 는 메일이 지워지거나 UIDVALIDITY 가 바뀌면 더 이상 같은 메일을 가리키지
않는다. 그래서 `archived_messages` 에 본문 HTML·텍스트·수신인까지 통째로 뜬다.
원본이 서버에서 사라져도 그대로 열린다 — 이게 이 기능의 존재 이유다.

**계정 FK 는 `onDelete: set null` 이다. cascade 로 바꾸면 안 된다.**
`/api/import` 가 백업 복원 시 `db.delete(schema.accounts).run()` 로 전 계정을 지우는데
(`foreign_keys = ON`), cascade 였다면 **백업 불러오기 = 보관함 전멸**이 된다.
계정이 사라지면 `sourceAccountId` 만 null 이 되고 사본과 출처 이름은 남는다.
목록에는 "(원본 없음)" 으로 표시한다.

표식(읽음/마크)도 보관 시점에 복사해 사본 자신의 컬럼에 둔다. `message_flags` 는
계정과 함께 cascade 로 사라지므로 계속 기댈 수 없다.

### 3.3 보관 본문은 클라이언트에서 받지 않는다

목록 행에는 본문이 없고, 무엇보다 클라이언트가 보낸 HTML 을 그대로 저장하면
`sanitize` 를 우회하는 길이 열린다. 방금 열어 본 것이면 `message-detail-cache` 에서,
아니면 IMAP 에서 다시 받아 온다.

본문은 SQLite TEXT 에 넣되 html 2MB / text 500KB 로 자른다. 넘치면 `data:` 로
박아 넣은 인라인 이미지부터 버린다. (파일 저장소를 새로 만들면 경로 설정·생성·
경로탈출 검사·고아 파일 정리가 통째로 딸려온다.)

**첨부와 `cid:` 인라인 이미지는 보관하지 않는다.** `MailMessageDetail` 에 첨부 필드가
없고 IMAP 구현도 읽지 않는다. 라이브 열람에서도 `cid:` 는 이미 깨져 있어 회귀는 아니다.

### 3.4 보관본 열람은 라이브 경로를 타면 안 된다

`/api/mail/[accountId]/[messageId]` 는 (a) 계정이 없으면 404, (b) IMAP 재조회,
(c) 열람과 동시에 읽음 표시 — 셋 다 사본에 맞지 않는다. 그래서 `/api/archive/[id]` 를
따로 둔다. 응답은 `MailMessageDetail` 과 **같은 모양**으로 맞춰 내려보낸다
(`toMailDetail`) — 모달이 두 모양을 알 필요가 없게.

### 3.5 캐시는 만료돼도 버리지 않는다

`mail-cache` 는 stale-while-revalidate 다. 만료된 값을 지우지 않고 그대로 보여준 뒤
뒤에서 갱신한다. IMAP 이 느리거나 죽어도 화면이 비지 않는다.
`mergeKeepingLastGood` 은 일부 계정만 실패했을 때 나머지 좋은 값을 지키는 장치다.

표식은 DB 라서 **캐시를 무효화하지 않고도** 즉시 반영된다(`withFlags`).
보관 여부도 같은 자리에서 얹는다.

### 3.6 순서 손잡이는 머리말이다

예전에는 dnd-kit 리스너와 `touch-action:none` 이 카드 전체에 걸려 있어 본문 글자를
고를 수도, 목록을 터치로 굴릴 수도 없었다. 지금은 `<header>` 에만 붙고, 안쪽 버튼·
링크 묶음은 `pointerdown` 을 막는다.

`role` / `tabIndex` 는 넘기지 않는다 — 버튼과 링크를 품은 머리말에 `role="button"` 을
씌우면 그 안의 것들이 보조기술에서 묻힌다.

### 3.7 메일 줄의 아이콘단은 흐름 밖에 있다

표식과 보관을 세로로 쌓으면 두 아이콘(24+24+간격)이 글자보다 키가 커서 줄 높이를
끌어올린다. 그래서 오른쪽에 띄워 두고, 예전에 표식 하나가 흐름 안에서 차지하던
폭(56px)을 그대로 비워 글자 자리가 달라지지 않게 했다.
`group/row` 는 **`<li>` 에 있어야 한다** — 아이콘단이 안쪽 div 의 형제라서,
안쪽에 두면 hover 가 걸리지 않는다.

### 3.8 시계 지역은 저장 값과 파생 값을 나눈다

`app_config.regions` 에 들어가는 것은 **이름 · 배지 · 위경도 · 표준시 · 단위 · 로케일**
뿐이다. 좌표 표기·표준시 라벨·날씨 조회 키는 `toRegion` 이 그릴 때 만든다. 손으로 적게
두면 위경도를 옮겼을 때 표기만 옛 값으로 남고, 서머타임이 바뀌어도 라벨이 안 따라온다.

`DEFAULT_REGIONS` 는 예전에 하드코딩돼 있던 두 곳(서울·West Lafayette) 그대로다.
**한 곳으로 줄이면 이미 쓰던 사람의 두 번째 시계가 업데이트만으로 말없이 사라진다.**

`normalizeRegions` 는 깨진 항목만 버리고 나머지는 살린다 — 하나 틀렸다고 전체를
기본값으로 되돌리면 다른 지역 설정까지 함께 날아간다.

예전 `widget-config.ts` 에는 `mapImage` / `markerX` / `markerY` 필드가 있었는데
**어디서도 그리지 않는 죽은 코드**였다. 지도를 갈아 끼워야 할 것처럼 보이지만 아니다.

### 3.9 route handler 의 리다이렉트는 상대 경로로 준다

`NextResponse.redirect(new URL("/login", req.url))` 은 **쓰지 않는다**.
standalone 빌드의 route handler 에서 `req.url` 의 오리진은 요청의 Host 가 아니라
서버가 바인드한 주소로 채워진다. Dockerfile 이 `HOSTNAME=0.0.0.0` 이므로 그대로
절대 URL 을 만들면 `Location: http://0.0.0.0:3000/login` 이 나가고 브라우저가
거기로 끌려간다. Host 헤더를 무엇으로 주든 똑같다.

미들웨어는 증상이 없다 — Next 가 같은 오리진이면 상대 경로로 정규화해 준다.
그래서 **route handler 에서만** 터지고, 그중에서도 세션 만료 후 자동 갱신
(`/api/auth/auto-renew`)과 로그아웃에서만 지나가므로 "가끔" 처럼 보인다.

`lib/redirect.ts` 의 `redirectTo()` 를 쓸 것. Location 은 상대 경로여도 되고
(RFC 9110 §10.2.2) 브라우저가 현재 오리진 기준으로 풀어 주므로 LAN·Tailscale·
리버스 프록시 어디로 들어왔든 따라온다. Host 헤더를 믿고 오리진을 되짜맞추는
방법도 있지만 그건 헤더 위조로 열린 리다이렉트가 되는 길을 새로 여는 셈이다.

### 3.10 마이그레이션은 `when` 이 증가해야만 적용된다

`drizzle/meta/_journal.json` 의 `when` 이 마지막 적용값 이하이면 drizzle 은 **예외 없이
조용히 건너뛴다.** `npm run db:generate` 후 새 항목의 `when` 을 눈으로 확인할 것.
`drizzle/` 는 커밋 대상이다.

### 3.11 Dockerfile 의 `ENCRYPTION_KEY` 는 빌드 단계 더미다

빌드 시점에만 필요해서 넣어 둔 값이고 **런타임 스테이지에는 없다**. 실제 값은
컨테이너 환경변수로 주입한다. 이미지에 시크릿은 들어가지 않는다.

---

## 4. 자주 건드리게 되는 곳

| 하고 싶은 일 | 손댈 곳 |
|---|---|
| 메일 목록에 새 표시 얹기 | `providers/types.ts` 의 `MailMessage` → `api/mail/route.ts` 의 `withFlags` → `inbox-card.tsx` |
| 보관 사본에 컬럼 추가 | `db/schema.ts` → `archive-server.ts`(Summary/Detail/values) → **export/import** |
| 위젯 추가 | `components/widget-*.tsx` → `widget-side-wing.tsx` 또는 `dashboard.tsx` 배치 |
| 시계 지역에 항목 추가 | `lib/regions.ts` 의 `RegionInput` → `normalizeRegions` → `settings/regions-setting.tsx` 입력칸. 파생 값이면 `toRegion` 에만 |
| 화면 색·글꼴 | `app/globals.css`. 인라인 style 로만 쓰는 변수는 `:root` 에 둘 것 |

---

### 3.12 `/api/login` 은 사람이 아닌 클라이언트용이다

화면 로그인은 서버 액션이라 폼 인코딩과 액션 ID 를 알아야 부를 수 있다. MCP 서버나
스크립트가 그걸 흉내내게 두면 Next 내부 규약에 묶이므로 JSON 입구를 따로 뒀다.
검사·기록·쿠키는 서버 액션과 같은 것을 쓴다. 폼이 아니라 무차별 대입이 쉬워지는
만큼 실패에 고정 지연을 준다.

---

### 3.13 본문을 여는 길은 **연결을 빌리고, 조각만 받는다**

메일을 처음 열 때 6.4초가 걸린다는 말에서 시작해 나눠 재 본 결과다
(흉내 낸 회선: 왕복 30ms · 2.5MB/s · LOGIN 300ms · SELECT 250ms · TLS).

| | 3KB 메일 | 14MB(10MB 첨부) |
|---|---|---|
| 연결+TLS+LOGIN | 514ms | 517ms |
| getMailboxLock (LIST·LSUB·SELECT) | 393ms | 391ms |
| 내려받기 | 60ms | 5,813ms |
| mailparser | 1ms | 262ms |

두 가지가 나왔다.

**하나. 실제로 하는 일은 FETCH 한 번인데 왕복이 아홉 번이었다.** 명령 로그를 세 보면
`ID · LOGIN · ID · NAMESPACE · LIST · LSUB · SELECT · FETCH · LOGOUT`. 메일 한 통을
열 때마다 이 아홉을 새로 했다. 그래서 **첨부·인라인 그림이 쓰던 연결 풀**
(`imap-pool.ts`)을 본문도 함께 쓴다. 데워진 연결에서는 아홉이 하나가 된다.
풀을 두 벌로 만들면 안 된다 — 같은 계정에 연결이 두 배로 열려 Gmail 의 동시 15
연결에 부딪힌다. 그래서 `mail-part.ts` 가 아니라 공용 자리에 있다.

**둘. 두 문단을 읽으려고 첨부까지 받아 왔다.** `source: true` 는 RFC822 원문을 통째로
끌어온다. 이제 첫 왕복에 구조(`bodyStructure`)와 원문 앞 128KB 를 같이 받고 —
짧은 메일은 여기서 끝나 예전과 **똑같은 길**을 간다 — 넘치면 건너뛸 바이트가 256KB
이상일 때만 본문 조각만 따로 받는다. 건너뛸 것이 없으면(그냥 큰 HTML) 128KB **뒤를
이어** 받는다.

조각만 받아도 **mailparser 를 버리지 않는다.** 조각마다 그 MIME 헤더를 붙여 같은
파서에 먹인다 — base64·quoted-printable 도, euc-kr·shift_jis 도 그대로 그쪽이 푼다.
주소·제목은 `BODY[HEADER]` 를 같은 파서에 먹인다.

**조각 하나만 먹일 때는 `skipHtmlToText: true` 를 켜야 한다.** html 조각을 홀로
파싱하면 그 조각이 mini 메시지의 **뿌리**가 되어 `hasText === false` 가 되고,
`getTextContent` 가 `(node.root && !this.hasText)` 가지를 타 `htmlToText()` 를
돈다 — 원문을 통째로 볼 때는 그 html 이 alternative 안이라 **한 번도 안 걸리는
줄이다.** 거기서 던지면 mailparser 가 `'error'` 를 내고 `simpleParser` 가 reject
하며, 그 예외가 라우트의 **500** 이 된다. 값이 조용히 달라지는 것이 아니라 **그
메일이 다시 열어도 영영 안 열린다.** 던지기 시작하는 깊이를 재 봤다: `<div>`
2,312겹 · `<blockquote>` 2,256겹 · `<table><tr><td>` 758겹. **보내는 쪽이 정하는
값**이라 아무 발신자나 만들 수 있다. 이 손잡이는 그 자리에서 빈 글자를 넣고
지나가고, 우리는 html 조각의 `one.text` 를 어차피 버리므로 잃는 것이 없다.
덤으로 버릴 글자를 짓느라 쓰던 값도 사라진다(672KB 소식지 129ms → 6ms).

그리고 `fetchBodyParts` 호출은 **try/catch 안에 있다.** 이 함수가 스스로 물러나는
표시는 `null` 뿐이라, 무엇이든 던지면 물러날 길이 없어진다. 조각 길은 빠르자고
만든 곁길이지 유일한 길이 아니다 — 실패하면 예전 길이 답한다. 지우지 마라.

**어느 모양에서 조각만 받아도 되는가는 `planBody` 하나가 정한다.** 이 자리를
`isBodyPart` 에게 물었더니 **본문이 조용히 사라졌다.** 두 함수는 다른 질문에
답한다 — `isBodyPart` 는 "첨부 목록에 세울 것인가", `planBody` 는 "mailparser 가
원문을 통째로 봤다면 무엇을 본문에 넣었을까" 다.

mailparser 는 조각을 이렇게 나눈다(`mail-parser.js` 의 `getTextContent`).
text/plain 은 text 에 넣되 **alternative 밖이고 html 이 있으면** `textToHtml` 한
것을 html 에도 **이어 붙인다**. text/html 은 html 에 넣되 alternative 밖이고
plain 이 있으면 `htmlToText` 한 것을 text 에도 넣는다. 우리는 html 끼리·text 끼리
이으므로, 이 **가로지르는 두 줄이 한 번도 안 걸릴 때만** 두 길의 결과가 같다.
`planBody` 가 재는 것이 그것이다.

실측으로 사라졌던 것들(A/B rig):

| 모양 | 사라진 것 |
|---|---|
| `mixed[ alternative[p,h], text/plain 꼬리말, 첨부 ]` | 꼬리말이 html 에서 통째로 |
| `mixed[html, plain, 첨부]` · `mixed[plain, html, 첨부]` | html·text 양쪽 |
| `multipart/report[ plain, message/delivery-status, … ]` | 반송 사유 (파서는 delivery-status 도 **본문**으로 센다) |
| `mixed[ plain, message/rfc822(inline), 첨부 ]` | 안쪽 메일 (mailsplit 이 `inline` 일 때만 안을 편다) |

물러나는 자리는 셋이다. **보관(`inlineImages: "embed"`)** — 인라인 그림의 바이트를
본문에 구워야 하는데 조각만 받으면 그 바이트가 없다. **본문이 alternative 밖에
형제로 흩어진 메일** — 위 표의 첫 두 줄. **`inline` 인 message/rfc822**.
느린 것이 사라진 것보다 낫다.

물러나는 값은 재 두었다: 첨부가 붙어 128KB 를 넘는 104통짜리 모양 표본에서
**88통(84.6%)이 조각 길로 간다.** 물러난 16통은 꼬리말 7 · 빈 리터럴 5 ·
inline rfc822 2 · 애초에 조각이 하나뿐인 메일 2 다. 이 비율이 떨어지면 `[mail]
본문 내역 … (물러남: …)` 로그가 사유별로 말해 준다.

`Content-Type: text/plain; name="notes.txt"` 처럼 **이름만 붙은 글자 조각은 이제
조각 길로 간다.** mailparser 는 이름을 보지 않고 disposition 만 보므로(그래서
그것도 본문이다) 어긋나지 않는다 — 앞에서 물러났던 것은 잘못된 조심이었다.

잃은 것 하나: 조각만 받은 메일의 **첨부 크기가 어림값**이다(파서가 바이트를 못 재니
서버가 준 인코딩 크기에서 환산). base64 의 줄바꿈까지 셈에 넣어 오차는 0.5% 안이다.
이 하나가 두 길의 **유일한** 차이다 — 나머지(html · text · 첨부 id/이름/타입 ·
제목 · 보낸이 · 추적 픽셀 수 · 프록시 수)는 글자까지 같다는 것을 표본 49통 ×
`link`/`embed` × 서버 다섯 벌로 확인했다.

첨부 **이름은 서버가 준 것을 먼저** 쓴다(`listAttachments`). 파서와 imapflow 는
`=?UTF-8?B?…?=` · `=?euc-kr?B?…?=` · RFC 2231 로 쪼갠 `filename*0*` 을 모두 같게
푼다. 갈리는 자리는 하나 — 헤더에 한글이 **날것 UTF-8** 로 박힌 메일(규격 위반이지만
흔하다)에서 파서는 그 바이트를 latin-1 로 읽어 `ëì©ë.pdf` 를 내놓고 imapflow 는
`대용량.pdf` 를 내놓는다. 파서를 먼저 쓰던 때는 **같은 메일이 크기에 따라 다른
이름으로 보였다**(조각 길이면 옳고, 보관·물러남이면 깨진 채로). 순서를 뒤집어
맞췄다. 서버가 구조에 이름을 안 실어 주면 `??` 가 파서 쪽으로 넘어간다.

**`RFC822.SIZE` 로 "다 받았다" 를 판단하면 안 된다.** 서버가 실제보다 작게 부르는
일이 있다 — LF 로 저장하고 CRLF 로 내주는 서버가 줄 수만큼 작게 답한다. 그 값을
믿으면 앞 128KB 만 쥐고도 다 받았다고 보아 **잘린 원문을 파서에 먹인다**(실측:
135,164바이트를 130,502 로 답한 서버에서 html 이 130,202 → 126,270 으로 잘렸다.
한글 한 자가 반 토막 난 채로, 오류도 신호도 없이). 믿을 수 있는 신호는 하나뿐이다
— **청한 것보다 적게 왔으면 거기가 끝이다.** 그래서 아예 청하지도 않는다.

`[mail] 본문 내역 … conn/head/more/parse/render … whole|resumed|parts (물러남: …)`
로그가 다음에 느려질 때 어디가 느린지 바로 말해 준다. `물러남` 은 **아낄 것이
있었는데 조각 길로 못 갔을 때만** 붙는다 — 조각이 하나뿐인 메일은 갈 곳이 없었던
것이지 물러난 것이 아니라서 안 붙인다.

**길이 0 인 리터럴을 조심할 것.** 조각을 받는 요청은 한 응답에 리터럴을 여럿 싣는
첫 자리다(`BODY[HEADER]` + 조각마다 둘). 그런데 서버가 그중 하나를 `{0}` 으로 주면
imapflow 의 셈이 어긋난다 — 스트림(`imap-stream.js`)은 빈 버퍼를 쌓아 두는데 토큰
파서(`token-parser.js`, "special case where literal content length is 0")는 그것을
꺼내 쓰지 않아, 그 뒤의 리터럴이 **한 칸씩 밀린다.** 2번 조각 자리에 1번 조각의
바이트가 들어앉아 **다른 글이 화면에 뜬다.** 밀린 자리는 값이 `undefined` 로 남는
것이 유일한 표시라, `fetchBodyParts` 가 그런 자리를 하나라도 보면 응답 전체를
버리고 예전 길로 물러난다(그때 `조각 응답이 밀렸다` 를 찍는다). 지우지 마라.

값이 있다: **빈 글자 조각을 끼운 메일이 전부 이 가드에 걸린다.** 알맹이가 없는
`text/plain` 을 alternative 에 넣는 HTML 소식지가 그렇고, 모양 표본에서 물러난
16통 중 5통이 이것이다. 없애려면 `size === 0` 인 조각은 아예 청하지 않으면 되는데,
그러면 **서버가 부르는 크기를 또 믿는** 셈이 된다(바로 위 `RFC822.SIZE` 문단이
그것 때문에 있다). 한 왕복을 아끼자고 그 문을 다시 열지 않았다.

### 3.14 놀던 연결은 건네기 전에 두드린다

소켓은 살아 있는데 답을 안 하는 연결이 있다. 상대가 제대로 끊으면 `close` 가 와서
풀에서 들어내는데, NAT 가 흐름 기록을 버리거나 서버가 멎으면 **아무 일도 일어나지
않는다.** 그런 연결을 빌려 가면 다음 명령이 `socketTimeout`(30초)을 꽉 채우고
실패한다.

이 고장은 **연결을 다시 쓰기 시작하면서 새로 생겼다.** 앞에서는 열 때마다 새
연결이라 이 길이 없었다. `borrowImapConnection` 의 재시도는 `getMailboxLock` 실패만
잡는데, 이미 SELECT 된 메일함이면 그 줄이 왕복을 한 번도 안 쓰고 지나가 `fetchOne`
에서 멎는다. 실측(흉내 서버가 소켓을 닫지 않고 답만 멈춘 뒤 5초 뒤 열기):
**30,002ms 실패**, 같은 순서로 옛 코드는 12ms 성공.

그래서 **1초 넘게 놀았던 연결은 건네기 전에 NOOP 으로 두드린다**(`imap-pool.ts` 의
`probe`). 답이 오면 그대로 쓰고, 5초 안에 안 오면 그 자리에서 끊고 새로 연다.
결과는 30,002ms 실패 → **5,034ms 성공**.

왜 매번이 아니라 1초인가 — 두드리는 값이 왕복 하나다. 인라인 그림 여덟 장처럼
잇달아 빌려 가는 자리에서 그 왕복이 여덟 번 는다. 이 고장은 **노는 동안** 생기므로
(풀에 최대 30초 머문다) 잇단 요청은 그냥 지나가게 두는 편이 맞다. 실측: 메일 5통을
잇달아 열면 1,678 → 1,678ms (그대로), 2초 간격으로 열면 1,683 → 1,936ms
(통당 +50ms 남짓).

왜 5초인가 — 잘못 버려도 값이 새 연결 하나(0.5초 남짓)뿐이라 짧게 잡고 싶지만,
**잘못 버리면 LOGIN 이 한 번 는다.** 이 파일이 막으려는 것이 바로 로그인 폭증이고
Gmail 은 로그인도 조인다. 서버가 잠깐 바쁜 것을 죽었다고 오해해 로그인을 퍼붓는
것보다, 드문 고장에서 5초를 쓰는 편이 낫다.

**하나가 죽은 것을 보면 놀고 있던 형제도 함께 버린다.** 이 고장은 소켓 하나만 골라
덮치지 않는다 — 노트북이 깨어나거나 NAT 가 기록을 버리면 같은 상대로 놀던 연결이
한꺼번에 벙어리가 된다. 그것들을 차례로 두드리면 5초가 사람 앞에서 쌓인다(실측:
풀 셋을 채운 뒤 통째로 벙어리가 되면 본문 열기 **15,065ms**). 하나가 죽은 자리에서
나머지 **놀던** 것을 두드리지 않고 버리면 같은 자리가 **5,034ms** 다. 쥐고 있는
연결(`busy`)은 안 건드린다 — 남의 일이 도는 중에 소켓을 닫으면 잘린 본문이 나온다.

### 3.15 수집기가 새 메일 본문을 **미리 받아 둔다**

위 두 절은 **다시 여는 것**을 빠르게 했다. 첫 열기는 여전히 IMAP 왕복이었다 —
캐시에는 사람이 한 번 열어 본 것만 있었으니까. 이제 `pollOnce` 의 마지막 단계
(`prefetchBodies`)가 새 메일 본문을 받아 캐시에 담는다. 첫 열기가 **925ms → 1ms**.

**에이전트에게 가는 것은 안 바뀐다.** `handOff` 는 제목·보낸이·날짜만 보낸다.
본문을 디스크에 받아 두는 것과 그것을 에이전트에게 먹이는 것은 **다른 일이다.**
순서로도 못 박혀 있다 — `prefetchBodies` 는 `handOff` **뒤에** 돌고 두 함수 사이에
값이 오가는 자리가 없다. 실측으로도 확인했다(가짜 에이전트가 받은 몸통이 켜나
끄나 630바이트로 글자까지 같다). 섞지 마라.

정해 둔 값과 그 이유:

- **한 번에 10통**(`PREFETCH_MAX`), **총 60초**(`PREFETCH_BUDGET_MS`). 통수만으로는
  모자란다 — 한 통이 `socketTimeout` 30초를 채울 수 있다. 시간은 **통 하나를
  시작하기 전에만** 본다. 도중에 끊으면 **잘린 본문이 캐시에 앉는다.**
- **한 통씩 줄 세워서.** 이게 연결 상한이다. 계정 10개에서 동시에 열린 연결
  최대치가 끄나 켜나 **10** — 봉투를 받느라 이미 열리는 계정당 하나가 천장이고
  미리 받기는 그 천장을 안 올린다.
- **3연속 실패면 접는다**(`PREFETCH_FAIL_STREAK`). 서버가 죽었거나 조이는 중이고,
  Gmail 은 반복 실패에 계정을 잠근다.
- **캐시 표를 갈라 센다** — 미리 받기 몫 30통, 사람 몫 바닥 70통, 합은 그대로 100.
  안 가르면 **미리 받은 것이 사람이 읽은 것을 밀어낸다**(실측: 사람 120통을 담근
  뒤 미리 받기 40통 → 옛 규칙에서는 사람 60/미리 40, 사람 본문 40통이 날아갔다).
  사람이 열면 `peekDetail` 이 1→0 으로 **승격**시킨다.
- **주입 흔적이 걸린 메일도 미리 받는다.** 그 판정은 "에이전트에 안 보낸다" 이지
  "사람이 못 본다" 가 아니다.
- `MAIL_PREFETCH=0|off|false|no` 로 끈다. 기본은 켜짐.

**보관은 미리 받은 본문으로 물러나면 안 된다**(`peekDetail(..., { humanOnly: true })`).
보관 라우트는 IMAP 이 죽으면 캐시로 물러나는데, 미리 받기 전에는 그 캐시에 사람이
열어 본 것만 있어 갈래가 좁았다. 이제 아무도 안 연 메일도 캐시에 있고, 그것은
`link` 판이라 그림이 **원본을 가리킨다** — 원본이 사라지면 깨진다. 바로 그것을
막으려고 `embed` 가 있다. 사람이 안 연 메일을 IMAP 이 죽은 사이에 보관하는 일은
드물고, 그때는 502 로 **왜 안 됐는지 말하는 편**이 깨질 본문을 말없이 굳히는 것보다
낫다. 덤으로 그 `peekDetail` 이 사람 몫 자리를 먹는 것도 함께 막힌다.

---

## 4.5 MCP 서버

`mcp/` 는 앱과 **별개의 npm 패키지**다 (루트 tsconfig 와 .dockerignore 에서 제외).

IMAP 에 직접 붙지 않고 HTTP API 만 쓴다 — 캐시·표식·보관 사본 규칙이 서버 쪽에 있다.
API 응답은 봉투가 라우트마다 다르다(`{message,flag}` / `{archived,list}` / `{list}` /
`{accounts}`). `shape.ts` 에서 벗겨 내고 필요한 것만 남긴다. 메일 본문은 HTML 이면
태그를 벗겨 텍스트로 준다 — 태그가 토큰을 다 먹는다.

---

## 5. 운영

```bash
npm run dev
npm run build && npm start
npm run db:generate
```

배포는 NAS 에서 `docker-compose up -d --build`. 코드 트리를 교체할 때는
`src` / `drizzle` / `public` 을 먼저 지우고 풀어야 한다.

`data/` 는 DB 가 사는 곳이다. **커밋 금지.** 백업은 sqlite `.backup` API 로 —
`cp` 는 WAL 내용을 놓친다.

ESLint 설정이 없어 `npm run lint` 는 대화형 설정 마법사를 띄운다. 지금은 타입체크와
빌드로만 검증한다.

---

## 6. 알려진 문제 / 남아 있는 것

- **`middleware.ts` 의 경계 검사** — `startsWith(p)` 가 경로 경계를 보지 않아
  `/loginX` 같은 것이 공개 취급된다. 지금 실제로 뚫리는 경로는 없지만 규칙 자체가 위험하다.
- **API 401** — 세션이 만료되면 API 요청도 `/login` 으로 302 된다. 클라이언트의
  `res.json()` 이 파싱 오류로 터진다. `/api/*` 는 401 JSON 을 주는 편이 맞다.
- **UIDVALIDITY 무방비** — `message_flags` 의 키가 `(account_id, message_id)` 인데
  UIDVALIDITY 를 저장하지 않는다. 서버가 UID 를 재사용하면 표식이 엉뚱한 메일에 붙는다.
  보관함은 스냅샷이라 오염이 누적되지는 않는다.
- **저장 실패 무통보** — 위젯 저장과 보관함 순서 저장이 `.catch(() => {})` 로 삼킨다.
- **미리 받기가 큰 메일을 미리 못 거른다** — 봉투에는 크기가 없어서, 1MB 상한을
  넘는 본문인지는 받아 본 뒤에야 안다. 헛수고는 통당 최대 한 번이다. 이제
  `imap.ts` 의 첫 왕복이 구조를 함께 받으니 그것으로 거를 여지는 있다.
- **미리 받는 동안 수집이 잠긴다** — `running` 이 최대 60초 잡혀, 그 창에 들어온
  수집 요청은 `skipped` 로 돌아간다. 10분 주기에서는 문제가 안 되지만 주기를
  줄이면 걸린다.
- **`fetchMessageFromClient` 는 죽은 코드다** — 본문 길이 풀을 쓰게 되면서 부르는
  곳이 없어졌다. 지울 때 MCP 쪽까지 함께 볼 것.
- **`isWide` 임계 1280px** — 위젯 날개가 752px 로 고정이라 그 근처에서 메일 카드가
  많이 좁아진다.
